{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "C/C++: gcc-13 build active file",
            "command": "mkdir -p \"${fileDirname}/bin\" && /usr/bin/gcc -fdiagnostics-color=always -g \"${file}\" -o \"${fileDirname}/bin/${fileBasenameNoExtension}\"",
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Compila o arquivo ativo para bin/"
        }
    ]
}